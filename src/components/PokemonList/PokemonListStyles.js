import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16
  },
  separator: {
    height: 10,
    backgroundColor: "transparent"
  },
  loading: {
    marginTop: 20,
    marginBottom: 60
  }
});
